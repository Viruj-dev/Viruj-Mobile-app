# Public URL only. Never put database credentials or private API keys here.
# Requires the mobile route aliases and bearer plugin from virujhealthapp.
EXPO_PUBLIC_WEB_API_URL=https://app.virujhealth.com
# Local Android emulator: http://10.0.2.2:3000 (development builds only).
# Physical device: use your computer's LAN IP and an isolated test database.

# Shared discovery and ERP appointments; auth/profile continue using the web origin.
EXPO_PUBLIC_PATIENT_API_URL=https://api.virujhealth.com
